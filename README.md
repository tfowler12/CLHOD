# Corporate Directory (with data)

Run locally:
```
npm install
npm run dev
```

Admin tools: add `?admin=1` to the URL.
Data file included: `public/data/directory.json` (first 260 rows from your CSV).
